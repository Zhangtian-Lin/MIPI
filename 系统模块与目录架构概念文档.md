# 马来西亚产业与政策情报平台：系统模块与目录架构概念文档

> 暂定英文名：Malaysia Industry & Policy Intelligence（MIPI）  
> 文档版本：v0.1  
> 日期：2026 年 8 月 20 日  
> 当前产品形态：Web  
> 后续产品形态：微信小程序及其他客户端

## 1. 文档目的

本文档定义项目的模块边界、仓库目录、依赖方向和跨平台复用策略，使系统能够先以网页形式上线，后续增加微信小程序时无需重写数据、业务规则和核心接口。

本文档重点回答：

1. 项目代码应按什么目录组织；
2. 哪些功能属于独立业务模块；
3. 网页、小程序、后台和采集系统如何共享能力；
4. 哪些代码可以复用，哪些必须平台独立；
5. 项目从单体向更大规模演进时如何拆分；
6. 如何避免目录结构随功能增加而失控。

## 2. 架构结论

项目首期建议采用：

> **Monorepo + 模块化单体后端 + 多客户端适配层 + 异步任务管道。**

总体结构为：

```text
网页 Web ─────────┐
管理后台 ─────────┼──> API / BFF ──> 业务模块 ──> PostgreSQL
微信小程序（后续）─┘          │              ├──> 对象存储
                              │              ├──> Redis / 队列
采集 Agent ──> Ingestion API ─┘              └──> 搜索索引（后续）
```

首期不采用大量微服务。所有核心业务模块运行在同一个后端应用中，但通过明确目录、接口和数据库所有权保持可拆分性。出现真实的性能、团队或部署需求后，再把采集、搜索、Agent 等模块拆成独立服务。

## 3. 跨平台复用原则

### 3.1 应当高度复用

- API 契约；
- 数据类型；
- API Client SDK；
- 权限与身份模型；
- 业务状态枚举；
- 金额、日期、来源等级和事实等级格式化规则；
- 搜索和筛选参数；
- 事件、项目、政策等页面所需的 View Model；
- 设计变量，如颜色、字号、间距和状态色；
- 埋点事件名称；
- 测试样本和接口契约测试。

### 3.2 可以部分复用

- 数据请求 Hooks；
- 缓存键和查询状态；
- 表单校验；
- 用户关注与收藏逻辑；
- 登录状态管理；
- 图表数据转换；
- Markdown 或富文本内容模型。

这些代码只有在网页与小程序技术栈兼容时才共享；不能为了复用而引入复杂的运行时适配。

### 3.3 不应强行复用

- DOM 组件；
- 网页路由实现；
- 浏览器专用 API；
- 小程序页面生命周期；
- 地图、分享、支付、登录等平台能力封装；
- 桌面端复杂表格和移动端轻量列表；
- 页面布局和交互细节。

跨端复用的重点是“数据、规则和接口”，不是追求所有 UI 代码完全相同。

## 4. 技术形态建议

技术选型可以在实施阶段调整，但模块职责应保持稳定。

### 4.1 前端

- Web：React / Next.js 类服务端渲染框架；
- 管理后台：React，与 Web 共享设计变量和 API SDK；
- 小程序：后续使用 Taro、原生微信小程序或其他适配方案；
- 共享层：TypeScript 类型、SDK、格式化工具和设计变量。

### 4.2 后端

- Python API 框架，如 FastAPI；
- Python 异步 Worker 执行采集、解析、翻译、抽取、聚类和索引；
- OpenAPI 作为前后端接口契约；
- PostgreSQL 作为权威业务数据库；
- pgvector 用于向量检索；
- S3 兼容对象存储保存网页、PDF、附件和历史快照；
- Redis 或任务队列用于缓存、异步任务和调度。

采用 Python 后端有利于复用 NLP、Embedding、聚类和数据处理生态；通过 OpenAPI 自动生成 TypeScript SDK，避免前后端手工维护两套字段定义。

## 5. 仓库顶层目录

建议目录：

```text
mipi/
├── apps/
│   ├── web/                    # 用户网页
│   ├── admin/                  # 数据审核与运营后台
│   ├── api/                    # HTTP API / BFF 启动入口
│   ├── worker/                 # 异步任务执行入口
│   ├── scheduler/              # 定时任务和采集计划入口
│   └── miniapp/                # 后续微信小程序
│
├── backend/
│   └── mipi/
│       ├── modules/            # 后端业务模块
│       ├── shared/             # 后端共享基础能力
│       └── bootstrap/          # 应用装配和依赖注入
│
├── packages/
│   ├── contracts/              # OpenAPI、JSON Schema、枚举定义
│   ├── sdk-ts/                 # 自动生成和封装的 TypeScript SDK
│   ├── shared-ts/              # 跨 Web/小程序的纯 TypeScript 工具
│   ├── view-models/            # 跨客户端展示数据模型
│   ├── design-tokens/          # 颜色、字号、间距和状态语义
│   ├── ui-web/                 # Web 通用组件
│   ├── ui-miniapp/             # 小程序通用组件，后续增加
│   ├── analytics-events/       # 统一埋点事件定义
│   └── test-fixtures/          # 共享契约样本和测试数据
│
├── infra/
│   ├── database/               # 数据库迁移、种子数据、视图
│   ├── object-storage/         # 对象存储配置
│   ├── queue/                  # 队列和任务路由配置
│   ├── search/                 # 搜索索引模板，后续启用
│   ├── containers/             # 本地和部署容器配置
│   ├── deploy/                 # 环境部署定义
│   └── monitoring/             # 日志、指标和告警配置
│
├── scripts/                    # 开发、数据维护和发布脚本
├── tests/
│   ├── contract/               # API 契约测试
│   ├── integration/            # 跨模块集成测试
│   ├── e2e-web/                # Web 端到端测试
│   └── e2e-miniapp/            # 小程序端到端测试，后续增加
│
├── docs/
│   ├── product/                # 产品与页面文档
│   ├── architecture/           # 系统、模块和数据架构
│   ├── operations/             # 采集与运营手册
│   ├── api/                    # API 使用说明
│   └── decisions/              # Architecture Decision Records
│
├── .env.example                # 环境变量名称，不保存密钥
├── README.md                   # 仓库入口
└── workspace configuration     # Monorepo 工作区配置
```

顶层目录按“可部署应用、可复用包、后端业务、基础设施、测试、文档”划分，而不是按编程语言随意堆放文件。

## 6. 可部署应用模块

### 6.1 `apps/web`

面向最终用户的网页应用，负责：

- 首页和重要变化；
- 产业、州属、企业、项目和政策页面；
- 治理、政党、选举和选区页面；
- 搜索与 Ask Malaysia；
- 用户关注、收藏与监测；
- SEO、公开分享和网页端性能优化。

建议内部目录：

```text
apps/web/
├── src/
│   ├── app/                    # 路由和页面装配
│   ├── features/               # 页面级功能模块
│   ├── widgets/                # 页面区块
│   ├── components/             # Web 基础组件
│   ├── hooks/                  # Web 专用 Hooks
│   ├── lib/                    # Web 运行时工具
│   └── styles/                 # Web 样式入口
├── public/
└── tests/
```

`features` 按业务组织：

```text
features/
├── changes/
├── industries/
├── states/
├── companies/
├── projects/
├── policies/
├── governance/
├── search/
├── ask/
└── workspace/
```

不要建立一个全局 `utils` 文件夹收纳所有无法分类的代码。

### 6.2 `apps/admin`

管理后台与公开网页独立部署，负责：

- 来源注册与健康状态；
- 文档和版本查看；
- 实体消歧；
- 事件合并和拆分；
- 关键字段校验；
- 来源与事实可信度审核；
- 政策 Diff 审核；
- 采集任务和失败处理；
- 内容更正与审计日志。

管理后台可以复用 Web 设计变量、SDK 和部分组件，但权限、路由和交互独立。

### 6.3 `apps/api`

API 是所有客户端的统一入口，负责：

- 身份验证和权限；
- 请求校验；
- 调用后端应用服务；
- 为不同客户端输出稳定 View Model；
- 分页、筛选和排序；
- API 版本控制；
- 限流、日志和追踪；
- 生成 OpenAPI 契约。

API 入口不直接包含复杂业务规则，也不允许控制器直接操作任意数据库表。

### 6.4 `apps/worker`

Worker 负责耗时或可重试任务：

- 网页和 PDF 解析；
- OCR；
- 语言识别和翻译；
- 实体抽取与标准化；
- 事件分类和聚类；
- Embedding；
- Policy Diff；
- 搜索索引更新；
- 通知和定期报告。

Worker 与 API 调用相同的后端应用服务，不复制业务规则。

### 6.5 `apps/scheduler`

Scheduler 只负责“何时触发什么任务”：

- 来源增量采集计划；
- 失败重试；
- 来源健康检查；
- 政策和项目定期复查；
- 每日聚合和趋势计算；
- 用户监测提醒。

调度器不负责实际采集和数据处理，防止调度与任务逻辑耦合。

### 6.6 `apps/miniapp`

后续微信小程序入口，首期可只保留 README 或空目录规划，不必提前实现。

小程序优先提供：

- 今日重要变化；
- 产业和州属轻量页面；
- 企业、项目和政策摘要；
- 搜索；
- Ask Malaysia；
- 关注、收藏和提醒；
- 分享卡片。

复杂 Policy Diff、大型地图、多列对比和管理功能仍以 Web 为主。

## 7. 后端业务模块

建议 `backend/mipi/modules` 包含：

```text
backend/mipi/modules/
├── sources/                    # 来源注册、评级和健康状态
├── ingestion/                  # 采集任务、抓取记录和接收入口
├── documents/                  # 原文、附件、版本和语言
├── processing/                 # 翻译、OCR、抽取处理记录
├── entities/                   # 标准实体、别名和消歧
├── events/                     # 事件、分类、聚类和时间线
├── companies/                  # 企业领域资料和关系
├── projects/                   # 项目状态、投资和参与方
├── policies/                   # 政策、版本、字段和 Diff
├── industries/                 # 产业分类和产业链
├── locations/                  # 州、城市、园区和地理层级
├── governance/                 # 政党、联盟、政府和任职
├── elections/                  # 选举、选区、候选人与结果
├── evidence/                   # 原子事实、证据和冲突
├── verification/               # S/F 分级、审核和更正
├── ranking/                    # 重要性排序和推荐
├── search/                     # 关键词、语义和混合检索
├── intelligence/               # Ask Malaysia 与分析任务
├── users/                      # 用户与身份
├── workspace/                  # 关注、收藏和监测
├── notifications/              # 消息与简报
└── audit/                      # 审计日志和数据变更记录
```

### 7.1 模块内部统一结构

每个后端模块采用相同结构：

```text
module_name/
├── domain/
│   ├── entities.py             # 领域实体和值对象
│   ├── rules.py                # 业务规则
│   ├── events.py               # 领域事件
│   └── ports.py                # 需要外部实现的接口
│
├── application/
│   ├── commands.py             # 写操作用例
│   ├── queries.py              # 读操作用例
│   ├── services.py             # 应用编排
│   └── dto.py                  # 模块边界数据对象
│
├── infrastructure/
│   ├── models.py               # 数据库映射
│   ├── repositories.py         # 仓储实现
│   ├── adapters.py             # 外部服务适配
│   └── tasks.py                # 异步任务适配
│
├── api/
│   ├── routes.py               # HTTP 路由
│   └── schemas.py              # 输入输出 Schema
│
└── tests/
```

不是每个小模块都必须从第一天建立所有文件；但新增代码必须明确属于领域、应用、基础设施或接口中的哪一层。

## 8. 核心模块职责

### 8.1 Sources

负责“从哪里来”：

- 来源机构、域名和账号；
- S1–S6 来源等级；
- 适用主题和权威范围；
- 来源访问规则；
- 更新频率和健康状态；
- 上游信源和转载依赖。

不负责保存完整正文，也不决定事件是否成立。

### 8.2 Ingestion

负责“如何进入系统”：

- 采集任务；
- 抓取尝试和响应元数据；
- URL 规范化；
- 幂等键；
- 失败重试和死信；
- 外部采集 Agent 的标准输入。

Ingestion 同时维护数据进入后的分层状态：

```text
L0 发现日志
L1 原始证据
L2 候选暂存
L3 权威情报
L4 公开投影
```

其中 L0–L2 由 Ingestion、Documents 和 Processing 协作管理；L3 由对应业务模块拥有；L4 由 API、Search 和客户端 View Model 生成。

### 8.3 Documents

负责“原文是什么”：

- 网页、PDF 和附件元数据；
- 原始对象存储地址；
- 文档版本；
- 原文、中文翻译和段落映射；
- 内容哈希；
- 发布、修改和抓取时间。

### 8.4 Entities

负责“涉及的是谁或什么”：

- 统一实体 ID；
- 标准名称和多语言别名；
- 实体合并、拆分和消歧；
- 实体类型；
- 跨模块实体引用。

Company、Policy、Project、Party 等领域模块保存自己的详细字段，但共享 Entities 提供的标准身份。

### 8.5 Events

负责“发生了什么变化”：

- 事件类型和时间；
- 同事件文档聚类；
- 事件与实体关系；
- 事件状态；
- 项目、政策、企业等对象的变化摘要；
- 时间线输出。

### 8.6 Evidence

负责“依据是什么”：

- 原子事实 Claim；
- 原文证据片段；
- 支持、反对、提及、替代和撤回关系；
- independence group；
- 关键字段不同来源口径。

### 8.7 Verification

负责“验证到什么程度”：

- F0–F4 事实验证等级；
- 来源与事实类型匹配；
- 部分确认和冲突状态；
- 人工审核任务；
- 更正、撤回和历史审计；
- 评分规则版本。

### 8.8 Intelligence

负责“如何形成综合回答”：

- 问题分解；
- 调用搜索和业务模块；
- 证据集合；
- 事实与分析分层；
- 回答引用；
- 有用性反馈。

Intelligence 不拥有企业、项目或政策事实，也不能绕过 Verification 直接修改可信度。

## 9. 后端共享基础能力

`backend/mipi/shared` 只放真正跨领域且没有业务归属的能力：

```text
shared/
├── database/                   # 连接、事务和基础迁移工具
├── messaging/                  # 事件总线和队列抽象
├── storage/                    # 对象存储抽象
├── cache/                      # 缓存抽象
├── auth/                       # 身份与通用授权组件
├── observability/              # 日志、指标和追踪
├── time/                       # 时区和时间工具
├── ids/                        # UUID / 公共 ID
├── errors/                     # 通用错误模型
└── config/                     # 配置读取
```

以下内容不能放入 `shared`：

- 项目状态判断；
- 政策版本规则；
- 事件重要性计算；
- 来源等级规则；
- 选举结果判断。

这些都属于具体业务模块。

## 10. 前端共享包

### 10.1 `packages/contracts`

权威接口契约，包含：

- OpenAPI 文档；
- JSON Schema；
- 公共枚举；
- 错误码；
- 分页和筛选规范；
- Webhook 或任务消息 Schema。

契约由后端生成或验证，前端不能自行复制一份不同定义。

### 10.2 `packages/sdk-ts`

由 OpenAPI 生成基础客户端，再进行轻量封装：

- 认证令牌注入；
- 错误标准化；
- 请求追踪 ID；
- 分页；
- 重试策略；
- Web 和小程序传输层适配。

SDK 不包含页面状态和 UI。

### 10.3 `packages/shared-ts`

只包含不依赖 DOM、Node.js 或微信运行时的纯函数：

- 日期和时区格式化；
- 金额与币种显示；
- S/F 等级标签映射；
- 项目状态和政策状态格式化；
- 搜索参数编码；
- 数据校验；
- 跨端埋点数据构造。

### 10.4 `packages/view-models`

定义面向客户端的稳定展示模型，例如：

```text
ImportantChangeCardVM
IndustrySummaryVM
StateOverviewVM
CompanyProfileVM
ProjectTimelineVM
PolicyDiffVM
EvidenceDrawerVM
ElectionResultVM
AskAnswerVM
```

View Model 已完成后端业务聚合，客户端不应分别请求多张表并自行重建可信度和统计口径。

### 10.5 `packages/design-tokens`

跨端共享语义，不直接共享 CSS：

```text
color.fact.confirmed
color.fact.conflict
color.fact.unverified
color.source.official
color.source.media
color.source.social
spacing.page
font.size.body
radius.card
```

Web 将 Token 编译为 CSS 变量，小程序转换为其可用的样式变量。

### 10.6 `ui-web` 与 `ui-miniapp`

两个 UI 包保持组件语义一致，但实现独立：

```text
Web: EvidenceDrawer
Miniapp: EvidenceSheet

Web: PolicyDiffTable
Miniapp: PolicyDiffCards

Web: StateInvestmentMap
Miniapp: StateInvestmentList / LightweightMap
```

共享的是数据模型和状态含义，不要求标签结构和交互完全一致。

## 11. 依赖方向

必须遵守：

```text
客户端页面
    ↓
View Model / SDK / Contracts
    ↓
API
    ↓
Application Use Case
    ↓
Domain Rules
    ↑
Infrastructure implements Domain Ports
```

禁止：

- Domain 导入 Web 框架或数据库 ORM；
- Web 直接访问数据库；
- Worker 直接改写其他模块拥有的表；
- 一个模块绕过对方 Application Service 操作其内部模型；
- `shared` 反向依赖具体业务模块；
- 小程序拥有一套独立业务规则；
- 在 UI 中重新计算可信度、投资总额或政策状态。

## 12. 模块间通信

### 12.1 同一进程内

首期使用明确的应用服务接口：

```text
Projects.register_announcement(...)
Events.attach_document(...)
Evidence.add_support(...)
Verification.request_review(...)
```

不允许一个模块直接修改其他模块数据库模型。

### 12.2 领域事件

需要异步处理的变化发布领域事件：

```text
DocumentVersionCreated
TranslationCompleted
EntitiesExtracted
EventClusterUpdated
ProjectStatusChanged
PolicyVersionPublished
ClaimConflictDetected
FactVerificationChanged
SearchReindexRequested
UserWatchMatched
```

### 12.3 Outbox

业务数据和待发布领域事件在同一数据库事务中写入 Outbox，再由 Worker 投递。这样可以避免数据库已经更新但队列消息丢失。

## 13. 数据库所有权

虽然首期共用一个 PostgreSQL，但每张表只能由一个模块负责写入。

示例：

| 表或数据 | 所有模块 | 其他模块访问方式 |
|---|---|---|
| `sources` | Sources | 查询接口或只读投影 |
| `documents`、`document_versions` | Documents | Document ID 和查询服务 |
| `entities`、`aliases` | Entities | Entity ID 和解析服务 |
| `events` | Events | Event API / 投影 |
| `projects` | Projects | Project Service |
| `policy_versions` | Policies | Policy Service |
| `claims`、`claim_evidence` | Evidence | Evidence Query |
| `verification_records` | Verification | Verification Service |
| `watchlists` | Workspace | Workspace Service |

其他模块可以通过稳定 ID 建立引用，但不能直接更新不属于自己的表。

## 14. API 设计

### 14.1 客户端无关的核心 API

核心资源 API 同时服务 Web 和小程序：

```text
GET /v1/changes
GET /v1/industries/{id}
GET /v1/states/{id}
GET /v1/companies/{id}
GET /v1/projects/{id}
GET /v1/policies/{id}
GET /v1/events/{id}
GET /v1/elections/{id}
GET /v1/search
POST /v1/ask
GET /v1/evidence/{claim_id}
```

### 14.2 客户端展示差异

优先通过请求参数控制内容密度：

```text
?view=summary
?view=full
?include=timeline,evidence
```

只有当 Web 与小程序需求明显分化时，再增加薄 BFF：

```text
/v1/web/home
/v1/miniapp/home
```

BFF 只做聚合和裁剪，不复制业务规则。

### 14.3 API 兼容

- 对外字段新增默认保持向后兼容；
- 不随意改变枚举语义；
- 删除字段必须经过弃用周期；
- 所有时间返回带时区的 ISO 8601；
- 所有金额返回原币、数值、币种和口径；
- 可信度同时返回来源等级、事实等级和状态，不只返回分数；
- 使用稳定公共 ID，不暴露可变数据库序号。

## 15. 采集 Agent 接入边界

采集 Agent 不直接写入业务表，而是提交标准 Ingestion Envelope：

```text
采集 Agent
    ↓
Ingestion API / Queue
    ↓
原始记录校验与幂等检查
    ↓
Documents 保存文档及版本
    ↓
Processing 创建处理任务
```

输入包含：

- 来源 ID 和 S 等级建议；
- 原始 URL 与规范化 URL；
- 发布、修改和抓取时间；
- 原文和对象存储地址；
- 内容哈希；
- 上游信源与 independence group；
- 相关性标签；
- F 等级建议；
- 审核标记。

S/F 等级的最终变更由 Sources 和 Verification 模块负责。

### 15.1 候选暂存区

无法放入当前网页分区但具有潜在价值的信息，不得强行塞入“其他新闻”，也不应直接丢弃。它进入候选暂存区：

```text
Ingestion Record
├── collection_relevance
├── verification_status
├── publication_status
├── retention_class
├── candidate_topics
├── suggested_destinations
├── staging_reason
├── review_after
└── review_flags
```

候选暂存区只对管理后台和审核流程可见，默认：

- 不进入公开搜索；
- 不进入首页和重要变化；
- 不参与产业、投资或选举统计；
- 不作为 Agent 已确认事实；
- 不修改权威实体当前状态；
- 可以被后续来源匹配并升级；
- 可以在新增分类或页面后重新路由。

### 15.2 三类状态独立

系统必须分开保存：

| 状态 | 回答的问题 |
|---|---|
| `collection_relevance` | 是否值得收集和保留 |
| `verification_status` | 具体事实验证到了什么程度 |
| `publication_status` | 是否以及在哪里对用户展示 |
| `retention_class` | 原始内容和记录保留多久 |

不能根据“当前没有页面”自动将 `collection_relevance` 设为低，也不能根据“已经保存原文”自动将 `publication_status` 设为可公开。

## 16. Web 到小程序的演进方式

### 阶段一：Web MVP

实施：

- `apps/web`；
- `apps/admin`；
- `apps/api`；
- `apps/worker`；
- `apps/scheduler`；
- `packages/contracts`；
- `packages/sdk-ts`；
- `packages/shared-ts`；
- `packages/design-tokens`。

此阶段就要避免 Web 直接依赖后端内部数据结构。

### 阶段二：小程序准备

- 检查核心 API 是否适合移动网络和小屏；
- 增加摘要 View Model；
- 抽离平台无关的请求和格式化代码；
- 将设计语义转换为小程序 Token；
- 设计微信登录与现有用户账户绑定；
- 明确分享卡片和页面路径。

### 阶段三：小程序上线

- 建立 `apps/miniapp`；
- 建立 `packages/ui-miniapp`；
- 复用 Contracts、SDK、Shared TS、View Models 和 Design Tokens；
- 为地图、富文本和证据查看提供小程序专用交互；
- 使用相同 API、用户关注和可信度规则。

### 阶段四：按真实需要优化

若小程序网络请求过多或首页结构明显不同，再增加 Miniapp BFF 或预聚合缓存，不复制核心业务逻辑。

## 17. 认证与用户体系

用户身份由后端 Users 模块统一管理：

```text
User
├── Web 登录身份
├── 微信 OpenID / UnionID
├── 邮箱或其他身份
└── Workspace / Watchlist
```

小程序用户不是另一套用户表。微信身份映射到统一 `user_id`，关注、收藏、历史查询和权限可以跨端同步。

## 18. 缓存和离线策略

### 18.1 Web

- 公开实体页可使用服务端缓存或 CDN；
- 用户工作区按用户隔离缓存；
- 证据和审核状态更新后主动失效相关页面；
- SEO 页面允许短时间缓存，但必须显示数据更新时间。

### 18.2 小程序

- 缓存最近查看的实体摘要；
- 缓存产业和州属基础目录；
- 事实等级、冲突和政策状态不得长期离线固定；
- 打开详情时重新验证关键状态；
- 离线内容明确显示最后更新时间。

## 19. 测试结构

### 19.1 单元测试

每个业务模块测试自己的规则：

- 项目状态转换；
- 政策版本；
- 来源等级匹配；
- 事实验证升级；
- 投资金额聚合；
- 选举结果状态；
- 事件去重。

### 19.2 契约测试

保证 Web、小程序和 API 对以下内容理解一致：

- 字段和枚举；
- 空值与未知状态；
- 时间与金额格式；
- S/F 等级；
- 分页与错误码；
- View Model。

### 19.3 集成测试

覆盖：

```text
采集记录 → 文档版本 → 抽取 → 事件 → 证据 → 验证 → API
```

### 19.4 端到端测试

Web 与小程序分别测试平台交互，但共享场景和期望数据：

- 查看今日变化；
- 进入项目时间线；
- 查看政策 Diff；
- 展开原文证据；
- 搜索企业别名；
- 关注实体并接收变化。

## 20. 配置与密钥

- 仓库只保存 `.env.example`；
- 密钥由运行环境或密钥管理服务提供；
- Web 公开环境变量与服务端密钥分开；
- 小程序 AppSecret 只能保存在服务端；
- 数据库、对象存储和模型密钥不得打包到客户端；
- 不同环境使用独立数据库和存储桶；
- 来源访问配置和用户凭证分开管理。

## 21. 日志与可观测性

所有可部署应用使用统一：

- `request_id`；
- `task_id`；
- `document_id`；
- `event_id`；
- `user_id`，日志中按隐私要求处理；
- 结构化错误码；
- 指标名称；
- 分布式追踪字段。

必须能够从网页中的一个事实追踪到：

```text
API 响应
→ View Model
→ Claim
→ Evidence
→ Document Version
→ Source
→ Crawl Task
```

## 22. 架构演进与服务拆分

首期模块化不等于微服务化。只有出现以下信号才考虑拆分：

| 信号 | 可能拆分模块 |
|---|---|
| 采集和 OCR 占用大量计算资源 | Processing Worker |
| 搜索规模和延迟独立增长 | Search Service |
| Agent 查询需要独立扩缩容 | Intelligence Service |
| 通知任务量显著增加 | Notification Service |
| 不同团队需要独立发布 | 对应业务模块 |
| 数据安全边界不同 | 身份或管理后台服务 |

拆分前必须满足：

- 模块接口已经稳定；
- 数据所有权清晰；
- 领域事件已经存在；
- 有监控证明当前部署是瓶颈；
- 拆分收益大于网络、运维和一致性成本。

## 23. 目录与代码治理规则

### 23.1 新功能归属检查

新增功能前回答：

1. 它改变哪个业务对象；
2. 哪个模块拥有该对象；
3. 它是领域规则、应用编排、基础设施还是接口；
4. 是否需要跨客户端共享；
5. 是否已经有相同能力；
6. 是否引入新的模块间依赖；
7. 是否需要领域事件或数据库迁移。

### 23.2 禁止的目录模式

- 巨大的 `common/` 或 `utils/`；
- 所有数据库模型放在单个文件；
- 所有 API 路由放在单个文件；
- Web 和小程序各复制一套类型；
- 业务规则散落在页面组件；
- Worker 任务中直接编写无法复用的业务逻辑；
- 为尚不存在的规模提前建立大量空微服务；
- 用 `misc`、`temp`、`new` 等无业务含义命名长期目录。

### 23.3 Architecture Decision Record

重要决策写入：

```text
docs/decisions/
├── 0001-use-postgresql-as-source-of-truth.md
├── 0002-use-openapi-for-client-contracts.md
├── 0003-modular-monolith-first.md
└── 0004-web-and-miniapp-ui-separate.md
```

每个 ADR 记录背景、决策、替代方案、影响和复查条件。

## 24. 首期最小目录

不需要第一天创建完整终态目录。V0/V1 可以从以下结构开始：

```text
mipi/
├── apps/
│   ├── web/
│   ├── admin/
│   ├── api/
│   ├── worker/
│   └── scheduler/
├── backend/
│   └── mipi/
│       ├── modules/
│       │   ├── sources/
│       │   ├── documents/
│       │   ├── entities/
│       │   ├── events/
│       │   ├── projects/
│       │   ├── policies/
│       │   ├── evidence/
│       │   └── verification/
│       └── shared/
├── packages/
│   ├── contracts/
│   ├── sdk-ts/
│   ├── shared-ts/
│   └── design-tokens/
├── infra/
│   └── database/
├── tests/
├── docs/
└── README.md
```

Governance、Elections、Search、Intelligence、Workspace 等模块在进入对应版本时再建立，但数据模型和公共 ID 从开始就预留扩展能力。

## 25. 模块验收标准

一个模块可以被认为边界清晰，需要满足：

- 有明确业务职责；
- 拥有自己的写入数据；
- 对外提供应用服务或事件；
- 不依赖客户端或数据库具体实现完成核心规则；
- 可以独立测试；
- 关键操作具备幂等性；
- 变更具有审计记录；
- API 和事件契约明确；
- 未来拆分服务时不需要重新定义业务边界。

跨平台能力可以被认为可复用，需要满足：

- 不依赖 DOM 或微信专用运行时；
- 输入输出类型稳定；
- Web 和小程序使用相同业务语义；
- 不包含平台专用路由和 UI 状态；
- 有契约测试覆盖。

## 26. 最终原则

系统的复用边界应当是：

```text
一个数据库事实源
一个业务规则体系
一套 API 契约
多个客户端实现
```

网页和小程序共享数据、身份、业务规则、接口、状态语义和设计语言；它们可以使用不同的页面布局和交互组件。

项目先以模块化单体快速上线，但目录、数据所有权、应用服务和领域事件从第一天保持清晰。这样后续无论增加小程序、移动 App、数据 API，还是把搜索和 Agent 拆成独立服务，都可以沿现有边界扩展，而不需要重写整个系统。
